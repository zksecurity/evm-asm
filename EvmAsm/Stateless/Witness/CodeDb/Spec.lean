/-
  EvmAsm.Stateless.Witness.CodeDb.Spec

  Placeholder for cpsTriple specs over the `CodeDb` Program.

  Final form mirrors `Witness.NodeDb.Spec`: given a well-formed
  `witness.codes`, after `build_code_db`, the `CODE_DB_BUCKETS`
  table holds `(keccak256(code_i), ptr_i, len_i)` for every entry.

  TODO(stateless-proofs).
-/

namespace EvmAsm.Stateless.Witness.CodeDb.Spec

end EvmAsm.Stateless.Witness.CodeDb.Spec
