/-
  EvmAsm.Stateless.ExecutionEngine.Spec

  Placeholder for cpsTriple specs over the ExecutionEngine
  subtree.

  Final form:

  - `execute_new_payload_request_spec` -- composing block-hash
                                          check + versioned-hash
                                          check + Block.Execute
                                          matches Python's
                                          `execute_new_payload_request`.
  - `decode_execution_requests_spec`   -- matches Python's
                                          `requests.py`.

  TODO(stateless-proofs).
-/

namespace EvmAsm.Stateless.ExecutionEngine.Spec

-- TODO(stateless-proofs): cpsTripleWithin for the
-- ExecutionEngine subtree.

end EvmAsm.Stateless.ExecutionEngine.Spec
